SELECT 
PAYMENT_DATE ,
 SUM(REGN_CHARGES),  
 SUM(ADMISSION_CHARGES),
 SUM(DOC_FEE),    
 SUM(TEST_FEE),        
 SUM(SERVICE_CHARGES),
 SUM(WARD_CHARGES),
 SUM(TOTAL_PAYABLE), 
 SUM(DISCOUNT),    
 SUM(AMT_TO_PAY)  as total amount
from payment
GROUP BY
payment_date
  